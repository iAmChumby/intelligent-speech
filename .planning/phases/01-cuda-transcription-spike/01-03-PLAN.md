---
phase: 01-cuda-transcription-spike
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - scripts/verify_cuda.py
autonomous: true
requirements:
  - CORE-02
  - CORE-03
  - CORE-05

must_haves:
  truths:
    - "python scripts/verify_cuda.py prints '[PASS] All Phase 1 success criteria met' and exits 0"
    - "Scripts calls register_cuda_dll_paths() before any ctranslate2 import"
    - "Script exits 1 (hard gate failure) if engine.actual_device != 'cuda'"
    - "Script reports transcription latency in seconds"
    - "Script verifies CPU fallback device is reported as 'cpu'"
    - "pytest tests/ -v exits 0 with full suite green"
    - "CI cloud workflow (ci-cloud.yml) is green on windows-latest runner (non-GPU tests pass)"
    - "CI GPU workflow (ci-gpu.yml) runs verify_cuda.py and exits 0 on self-hosted [self-hosted, windows, gpu] runner"
  artifacts:
    - path: "scripts/verify_cuda.py"
      provides: "Walking skeleton entry point — exercises all 4 Phase 1 success criteria"
      exports: ["main"]
  key_links:
    - from: "scripts/verify_cuda.py"
      to: "backend/transcription/cuda_setup.register_cuda_dll_paths"
      via: "called before any ctranslate2 import on line 1 of effective imports"
      pattern: "register_cuda_dll_paths\\(\\)"
    - from: "scripts/verify_cuda.py"
      to: "TranscriptionEngine(device='cuda', compute_type='float16')"
      via: "SC1 + SC2 verification block"
      pattern: "compute_type.*float16"
    - from: "scripts/verify_cuda.py"
      to: "engine.transcribe_file(wav_path)"
      via: "SC3 timed transcription block"
      pattern: "transcribe_file"
---

<objective>
Implement scripts/verify_cuda.py — the Phase 1 Walking Skeleton entry point — and confirm all four success criteria pass on the target RTX 5070 Ti hardware.

Purpose: The verification script is the definitive Phase 1 acceptance gate. It exercises the complete CUDA stack end-to-end: DLL registration → model load → GPU device assertion (hard gate) → timed transcription → CPU fallback detection. Running this script and seeing four PASS lines is the definition of Phase 1 complete.

Output: scripts/verify_cuda.py that exits 0 on success (CUDA path) or exits 1 on hard gate failure (device != cuda).
</objective>

<execution_context>
@C:/Users/Luke/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Luke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Luke/Projects/intelligent-speech/.planning/ROADMAP.md
@C:/Users/Luke/Projects/intelligent-speech/.planning/STATE.md
@C:/Users/Luke/Projects/intelligent-speech/.planning/phases/01-cuda-transcription-spike/01-CONTEXT.md
@C:/Users/Luke/Projects/intelligent-speech/.planning/phases/01-cuda-transcription-spike/01-RESEARCH.md
@C:/Users/Luke/Projects/intelligent-speech/.planning/phases/01-cuda-transcription-spike/01-VALIDATION.md
@C:/Users/Luke/Projects/intelligent-speech/.planning/phases/01-cuda-transcription-spike/01-01-SUMMARY.md
@C:/Users/Luke/Projects/intelligent-speech/.planning/phases/01-cuda-transcription-spike/01-02-SUMMARY.md

<interfaces>
<!-- Contracts from Wave 2 that verify_cuda.py calls. -->

From backend/transcription/cuda_setup.py:
```python
def register_cuda_dll_paths() -> None:
    """Must be called before any ctranslate2 or faster_whisper import."""
```

From backend/transcription/engine.py:
```python
class TranscriptionEngine:
    def __init__(self, model_size: str, device: str, compute_type: str) -> None: ...
    def wait_until_ready(self, timeout: float = 60.0) -> bool: ...
    @property
    def actual_device(self) -> Optional[str]: ...
    def transcribe_file(self, path: str) -> str: ...
    @property
    def is_ready(self) -> bool: ...
    _load_error: Optional[Exception]
```

CUDA invocation for Phase 1 (per D-09, D-11, RESEARCH.md):
    TranscriptionEngine(
        model_size="large-v3-turbo",
        device="cuda",
        compute_type="float16",   # NOT int8_float16 — Blackwell hard requirement
    )

CPU fallback invocation (for SC4):
    TranscriptionEngine(
        model_size="large-v3-turbo",
        device="cpu",
        compute_type="int8",
    )
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Implement scripts/verify_cuda.py — Walking Skeleton entry point</name>
  <files>
    scripts/verify_cuda.py
  </files>

  <read_first>
    - C:/Users/Luke/Projects/intelligent-speech/.planning/phases/01-cuda-transcription-spike/01-RESEARCH.md
      Section: "Walking Skeleton Design" — complete verify_cuda.py blueprint with main() function
      Section: "What engine.py Must Implement for the Skeleton" — 5 interface methods required
      Section: "Verifying Actual Device Without Exceptions" — hard gate pattern (sys.exit(1) if actual != "cuda")
      Section: "Pitfall 4: Silent CPU Fallback" — why the hard gate is necessary
    - C:/Users/Luke/Projects/intelligent-speech/.planning/phases/01-cuda-transcription-spike/01-CONTEXT.md
      Section: "Transcription Stack" — D-11 (Blackwell hard gate: GPU is required for Phase 1)
    - C:/Users/Luke/Projects/intelligent-speech/.planning/phases/01-cuda-transcription-spike/01-VALIDATION.md
      Section: "Manual-Only Verifications" — the 4 behaviors this script must verify
  </read_first>

  <action>
Create scripts/verify_cuda.py implementing the Phase 1 verification entry point as specified in RESEARCH.md "Walking Skeleton Design" section.

The script exercises all four Phase 1 success criteria (SC1–SC4) from ROADMAP.md:
- SC1: Device is detected and printed (CUDA or CPU, no silent fallback)
- SC2: large-v3-turbo loads in a background thread; warmup inference completes without hanging
- SC3: test_speech.wav transcribed in < 3 seconds on CUDA
- SC4: CPU fallback loads and clearly identifies device="cpu"

Import order is MANDATORY — ctranslate2 cannot be imported before register_cuda_dll_paths():

    # Line 1 of functional imports (after docstring and stdlib imports):
    from backend.transcription.cuda_setup import register_cuda_dll_paths
    register_cuda_dll_paths()
    # Only after this call can ctranslate2/faster_whisper be imported:
    from backend.transcription.engine import TranscriptionEngine

Script structure (per RESEARCH.md blueprint):

    def main() -> int:
        print banner
        
        # SC1 + SC2: Load CUDA engine, verify device, confirm warmup
        engine = TranscriptionEngine("large-v3-turbo", device="cuda", compute_type="float16")
        ready = engine.wait_until_ready(timeout=120.0)
        if not ready: print "[FAIL] ..."; return 1
        if engine.actual_device != "cuda":
            print "[HARD GATE FAIL] ..."
            print "Phase 1 requires GPU transcription. Check CUDA installation."
            return 1
        print f"[PASS] Model loaded on device={engine.actual_device}"
        print "[PASS] CUDA JIT warmup completed (silence inference during load)"
        
        # SC3: Transcribe test WAV in < 3 seconds
        wav_path = "tests/fixtures/test_speech.wav"
        t0 = time.perf_counter()
        transcript = engine.transcribe_file(wav_path)
        elapsed = time.perf_counter() - t0
        if elapsed > 3.0: print f"[FAIL] Transcription took {elapsed:.2f}s (limit: 3s)"; return 1
        print f"[PASS] Transcription in {elapsed:.2f}s: '{transcript}'"
        
        # SC4: CPU fallback
        cpu_engine = TranscriptionEngine("large-v3-turbo", device="cpu", compute_type="int8")
        cpu_engine.wait_until_ready(timeout=120.0)
        if cpu_engine.actual_device != "cpu": print "[FAIL] ..."; return 1
        print f"[PASS] CPU fallback device={cpu_engine.actual_device} (clearly identified)"
        
        print "[PASS] All Phase 1 success criteria met"
        return 0

    if __name__ == "__main__":
        sys.exit(main())

The script must be runnable from the project root with:
    python scripts/verify_cuda.py

Add a shebang line comment (not an actual shebang — Windows doesn't use them):
    # Run from project root: python scripts/verify_cuda.py

Add error handling for the WAV fixture missing:
    if not os.path.exists(wav_path):
        print(f"[FAIL] Test fixture not found: {wav_path}")
        print("Run Wave 1 first to generate tests/fixtures/test_speech.wav")
        return 1

Print timing for both the CUDA load and the CPU load for observability:
    print(f"[INFO] CUDA engine ready in {cuda_load_elapsed:.1f}s")

SC4 CPU fallback: the CPU model load for large-v3-turbo may take 30–90 seconds without GPU. This is expected. The timeout in wait_until_ready is 120s which covers this. Print a progress indicator:
    print("[4] Loading large-v3-turbo on CPU (this may take 30-90s)...")

CRITICAL: The verify_cuda.py script itself does NOT enforce that CPU fallback is a failure — that is already handled by the SC1+SC2 CUDA hard gate. SC4 specifically tests that when CPU is intentionally requested, the device is accurately reported as "cpu". If SC4 fails, it means actual_device is None or incorrect, not that CPU itself is bad.
  </action>

  <verify>
    <automated>cd C:/Users/Luke/Projects/intelligent-speech && python scripts/verify_cuda.py 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - scripts/verify_cuda.py exists
    - First functional import in the file is "from backend.transcription.cuda_setup import register_cuda_dll_paths" followed immediately by "register_cuda_dll_paths()"
    - Script contains "compute_type.*float16" for the CUDA engine instantiation (NOT int8_float16)
    - Script contains "compute_type.*int8" for the CPU fallback engine instantiation (CPU uses int8 — that is correct)
    - Script exits 0 when run on RTX 5070 Ti with CUDA stack installed correctly
    - Script exits 1 with "[HARD GATE FAIL]" message when actual_device != "cuda"
    - Script prints "[PASS] All Phase 1 success criteria met" on success
    - Script covers all 4 success criteria from ROADMAP.md Phase 1 section
    - "python scripts/verify_cuda.py" runs from project root without ImportError or ModuleNotFoundError
  </acceptance_criteria>

  <done>python scripts/verify_cuda.py prints four [PASS] lines and "[PASS] All Phase 1 success criteria met", exits 0. Script exits 1 with hard gate message if GPU not available.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Hardware verification — confirm CUDA stack passes all 4 success criteria on RTX 5070 Ti</name>

  <what-built>
    Wave 1 established the project structure and dependency manifest. Wave 2 implemented cuda_setup.py and engine.py. Wave 3 Task 1 created scripts/verify_cuda.py. This checkpoint confirms the full stack works on your RTX 5070 Ti (Blackwell, sm_120) hardware.
  </what-built>

  <how-to-verify>
    ── Local verification ──────────────────────────────────────────

    1. Activate the virtual environment (if not already active):
       .venv\Scripts\Activate.ps1

    2. Install all dependencies (if not done during Wave 1/2 execution):
       pip install -r requirements.txt
       pip install -r requirements-dev.txt

    3. Run the Phase 1 verification script from the project root:
       python scripts/verify_cuda.py

    Expected output (all four lines should appear):
       [PASS] Model loaded on device=cuda
       [PASS] CUDA JIT warmup completed (silence inference during load)
       [PASS] Transcription in X.XXs: '...'   (X.XX must be < 3.0)
       [PASS] CPU fallback device=cpu (clearly identified)
       [PASS] All Phase 1 success criteria met

    4. Run the full pytest suite:
       python -m pytest tests/ -v

    Expected: All tests pass or xfail. Exit code 0.

    5. If verify_cuda.py fails with [HARD GATE FAIL] or a RuntimeError, check:
       - nvidia-smi output: confirm RTX 5070 Ti is detected
       - python -c "import ctranslate2; print(ctranslate2.__version__)" should print 4.7.1
       - python -c "import ctranslate2; print(ctranslate2.__version__)" — if it prints [WinError 3], the 4.7.1 fix did not apply; pin to ctranslate2==4.6.3 instead
       - If CUBLAS_STATUS_NOT_SUPPORTED appears: verify compute_type="float16" is used, NOT int8_float16

    ── CI verification (GitHub Actions) ───────────────────────────

    6. Push the project to a GitHub repository (create one if needed):
       git remote add origin https://github.com/<your-username>/intelligent-speech.git
       git push -u origin main

    7. Verify the cloud CI workflow passes automatically:
       - Go to GitHub → Actions tab → "CI — Non-GPU (cloud)"
       - Confirm the workflow run on windows-latest completes green
       - The "Run non-GPU tests" step should show all tests pass or xfail

    8. Register a GitHub Actions self-hosted runner on this machine (RTX 5070 Ti):
       - Go to GitHub repo → Settings → Actions → Runners → New self-hosted runner
       - Select: Windows, x64
       - Follow GitHub's instructions to download and configure the runner agent
       - When prompted for labels, add: gpu  (GitHub pre-adds "self-hosted" and "windows")
       - Start the runner: run.cmd (or install as a Windows service for persistence)
       - Confirm runner status shows "Idle" in GitHub → Settings → Actions → Runners

    9. Trigger the GPU CI workflow (push a commit or re-run from Actions tab):
       - Go to GitHub → Actions tab → "CI — GPU (self-hosted)"
       - Confirm the workflow run on [self-hosted, windows, gpu] completes green
       - The "Run Phase 1 verification script (source of truth gate)" step must exit 0

    ── Troubleshooting ─────────────────────────────────────────────

    Self-hosted runner PATH issues: if pip/python are not found in the runner, edit
    .github/workflows/ci-gpu.yml to use absolute paths or add a step that activates
    the project's virtual environment before running tests:
       - name: Activate venv and run tests
         run: |
           .venv\Scripts\Activate.ps1
           python -m pytest tests/ -v
           python scripts/verify_cuda.py
         shell: pwsh

    10. Report the exact output of scripts/verify_cuda.py and both CI workflow
        run URLs below.
  </how-to-verify>

  <resume-signal>
    Type "approved" if all four [PASS] lines appear locally, pytest exits 0, and
    both GitHub Actions workflows (cloud non-GPU + self-hosted GPU) are green.
    Or describe the exact failure output if any step fails — include the full error message.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scripts/verify_cuda.py → filesystem | Reads tests/fixtures/test_speech.wav — file path is hardcoded, not user input |
| scripts/verify_cuda.py → GPU hardware | Calls ctranslate2 via faster-whisper — no network access, local inference only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Denial of Service | TranscriptionEngine CPU load in SC4 | accept | SC4 CPU load runs in same process as CUDA check; 120s timeout prevents infinite hang |
| T-01-07 | Information Disclosure | verify_cuda.py stdout output | accept | Output contains device strings, timing, and transcript of test audio — no PII; local CLI tool |
</threat_model>

<verification>
After Wave 3 completes, Phase 1 is complete when ALL of the following are true:

1. `python scripts/verify_cuda.py` prints "[PASS] All Phase 1 success criteria met" and exits 0
2. `python -m pytest tests/ -v` exits 0
3. ROADMAP.md Phase 1 success criteria confirmed:
   - SC1: Script prints active compute device without silent fallback — VERIFIED by [PASS] Model loaded on device=cuda
   - SC2: large-v3-turbo loaded in background thread with warmup — VERIFIED by [PASS] CUDA JIT warmup completed
   - SC3: test WAV transcribed in < 3 seconds on CUDA — VERIFIED by [PASS] Transcription in X.XXs
   - SC4: CPU fallback identified explicitly — VERIFIED by [PASS] CPU fallback device=cpu
4. scripts/verify_cuda.py contains "float16" for CUDA engine, not int8_float16
5. GitHub Actions cloud CI (ci-cloud.yml on windows-latest) is green — non-GPU tests pass
6. GitHub Actions GPU CI (ci-gpu.yml on self-hosted [self-hosted, windows, gpu]) is green — verify_cuda.py exits 0
7. Human checkpoint (Task 2) approved with "[approved]" signal (includes both local and CI verification)
</verification>

<success_criteria>
- scripts/verify_cuda.py exits 0 on target hardware with four [PASS] lines
- Script exits 1 (hard gate) if device != "cuda" — Blackwell GPU requirement enforced
- Full pytest suite (tests/ -v) exits 0
- CI cloud workflow (ci-cloud.yml) is green on GitHub-hosted windows-latest runner
- CI GPU workflow (ci-gpu.yml) is green on self-hosted runner with RTX 5070 Ti — this is the Phase 1 source-of-truth gate
- Phase 1 acceptance gate is clear and reproducible: push to main → ci-gpu.yml green
</success_criteria>

<output>
After completion, create .planning/phases/01-cuda-transcription-spike/01-03-SUMMARY.md
</output>
